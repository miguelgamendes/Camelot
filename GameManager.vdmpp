class GameManager
/*
	Data structure responsible for managing the game
*/

	instance variables
		private board : Board := new Board()
		
	operations
		public run : () ==> ()
		run() == (
			board.begin();
			
			board.print();
		);

end GameManager